\* Models the CURRENT gc-v2 code (issue #429): the verifier consumes the
\* KickoffSent message and skips the Challenge when GOAT SPV lags the kickoff
\* height, with no defer/retry - expected to FAIL (fail-open). This is a live
\* bug, not a historical artifact.
SPECIFICATION FairSpec
CHECK_DEADLOCK FALSE
INVARIANT TypeOK
INVARIANT NoUnauthorizedTake1
