\* ORIGINAL #429 @ c15f1714: the verifier consumes the KickoffSent message
\* and skips the Challenge when GOAT SPV lags the kickoff height, with no
\* defer/retry - expected to FAIL (fail-open). Fixed by #455 (978f4201):
\* handle_kickoff_sent_verifier now defers via
\* push_local_unhandled_messages_with_reason(GoatSpvPending). Frozen as a
\* regression record; see VerifierKickoffFailOpenFixed.cfg for the fix design.
SPECIFICATION FairSpec
CHECK_DEADLOCK FALSE
INVARIANT TypeOK
INVARIANT NoUnauthorizedTake1
