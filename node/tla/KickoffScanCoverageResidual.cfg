\* DEPTH-LIMIT RESIDUAL of #451 (root-only entry): a chain of idle decoys
\* deeper than the walk cap. ScanDepth=2 with 4 graphs (real analogue: >32
\* decoys, kick beyond depth 32). Expected to FAIL: kicking the graph beyond
\* the depth budget escapes the single walk from the root. Superseded by the
\* all-pending-entries scan (see KickoffScanCoverageAllEntries.cfg); frozen
\* as the regression record of the root-only design.
CONSTANTS NumGraphs = 4  ScanDepth = 2  ScanAllPending = FALSE
SPECIFICATION Spec
INVARIANT TypeOK
INVARIANT KickoffAlwaysCovered
