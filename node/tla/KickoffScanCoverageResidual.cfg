\* DEPTH-LIMIT RESIDUAL: a chain of idle decoys deeper than the scan cap.
\* ScanDepth=2 with 4 graphs (real analogue: >32 decoys, kick beyond depth 32).
\* Expected to FAIL: kicking the graph beyond the depth budget still escapes.
CONSTANTS NumGraphs = 4  ScanDepth = 2
SPECIFICATION Spec
INVARIANT TypeOK
INVARIANT KickoffAlwaysCovered
