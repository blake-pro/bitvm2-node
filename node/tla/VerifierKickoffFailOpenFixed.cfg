\* The issue's suggested fix: defer (push_local_unhandled_messages_with_reason)
\* while SPV lags, so the Challenge is retried and actually sent before the
\* operator's Take1 - expected to PASS.
SPECIFICATION FairSpecFixed
CHECK_DEADLOCK FALSE
INVARIANT TypeOK
INVARIANT NoUnauthorizedTake1
